vcl 4.0;

backend default {
    .host = "{{ servers.exampleapp.ip }}";
    .port = "{{ servers.exampleapp.port }}";
}

sub vcl_recv {
	return (pass);
}

sub vcl_backend_response {
}

sub vcl_deliver {
}